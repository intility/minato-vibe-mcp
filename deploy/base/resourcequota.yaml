---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: minato-vibe-mcp-default
spec:
  hard:
    requests.cpu: "500m"
    requests.memory: 512Mi
    limits.cpu: "1"
    limits.memory: 1Gi
    pods: "5"
    services: "3"
    persistentvolumeclaims: "0"
